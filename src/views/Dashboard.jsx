/* import { useNavigate,Route, Routes, Link } from 'react-router-dom'
import React from 'react'

const Dashboard = () => {
const navigate = useNavigate()

const handleClick= ()=>{
    navigate('/')
}
return (
    <div><h1>Dashboarddddddddddd</h1>
        <button onClick={handleClick}>
            LogOut
        </button>
        <Link to='welcome'>
            Vamos a Welcome
        </Link>
        <Link to='/dashboard'>
            Vamos a dashboard
        </Link>
        <Routes>
        <Route path='welcome' element={<h1>Que taaaaal</h1>}/>
        </Routes>
    </div>
  )
}
 */
export default Dashboard